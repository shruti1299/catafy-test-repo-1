import { notification } from "antd";

const showNotification = (type:string, message: any, duration = 3) => {
    // (notification[(type as  any)]  )({
    //     message: message,
    //     duration: duration,
    // });
};

export default showNotification;
